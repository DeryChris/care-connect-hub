# TODO: Build HMIS Pages

## Completed Tasks:
- [x] Make the currency sign change in all places used across the app when currency is updated in Settings
- [x] Make GHS (₵) the default currency
- [x] Reduce the size of the Patients by Department pie chart card
- [x] Center the chart on the card and bolden it
- [x] Put the legend of the chart underneath without the numbers
- [x] Reduce the size of the At a Glance and Today's Summary cards
- [x] Place the Recent Activities card on top below the stats cards section
- [x] The favicon is now showing in the browser tab
- [x] Add a department field for patients in constants.ts
- [x] Improved CSS for better UI (rounded corners, fluid scrolling, better text visibility)
- [x] Quick Links and Departments cards placed side by side

## Remaining Tasks:
- [ ] 

## Tasks with Requirements
- [ ] Make all text visible across the app (especially green text over light green background)v (requires ui theme change)
- [ ] Make it so that if i apply any Saved Changes, it takes effect in the respect areas (requires backend database)
- [ ] Make sure all functions represented in the app works and functions properly (requires backend database)
- [ ] Build custom dashboard pages for each Designation based on module permissions (requires user permission system)
- [ ] Make sure information displayed is real info fetched from the database (requires backend database)
